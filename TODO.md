# TODO

## Priority Order (Alpha-first)

1. Auth + roles minimum viable
   - Login/logout, session protection, admin vs user.
2. Core data model for gems
   - Finalize gem schema and required validation rules.
3. Gems list + create/edit/delete
   - Complete CRUD with sorting, basic filters, and pagination.
4. File attachments on gem
   - Upload, list, delete, and cover image.
5. Search + filters
   - Global search and dynamic filtering (type/status/price/weight/date).
6. History/audit trail
   - Track create/update/delete with user, date, changed fields.
7. Selections (basic flow)
   - Create selection, add/remove gems, start with simple types first.
8. CSV import
   - Header validation, preview, valid rows imported, invalid rows rejected with reasons.
9. Export
   - CSV exports first, PDF export after core flows are stable.
10. Contacts module

- Add after core inventory and selection workflows.
